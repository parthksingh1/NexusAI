{
  "name": "@nexusai/rag",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --host 0.0.0.0 --port 5000 --reload",
    "build": "echo 'python service — no build step'",
    "lint": "python -m ruff check app",
    "typecheck": "python -m mypy app || true"
  }
}
