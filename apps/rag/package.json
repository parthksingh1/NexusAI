{
  "name": "@nexusai/rag",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": ".\\.venv\\Scripts\\uvicorn app.main:app --host 0.0.0.0 --port 5000 --reload",
    "build": "echo 'python service — no build step'",
    "typecheck": "mypy app || true"
  }
}
